<UserControl x:Class="HoldSpace.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HoldSpace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700"
             Background="#0C0C0D"
             SizeChanged="UserControl_SizeChanged">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Overview" Foreground="#F3F3F3" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Monitor hold launcher configuration and quick start settings." Foreground="#8E8E93" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Overview Dashboard Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="CardsGrid">
                <!-- Main Status Card -->
                <Border x:Name="CardStatus" Background="#131315" BorderBrush="#262629" BorderThickness="1" CornerRadius="12" Padding="20" Margin="8">
                    <StackPanel>
                        <TextBlock Text="ACTIVE TRIGGER KEY" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,16"/>
                        <TextBlock x:Name="TxtTriggerKey" Text="Caps Lock" Foreground="#0A84FF" FontSize="28" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtTriggerDelay" Text="120ms hold threshold" Foreground="#8E8E93" FontSize="12" Margin="0,8,0,0"/>
                        <Border Background="#1C1C1F" CornerRadius="6" Padding="10,8" Margin="0,16,0,0">
                            <TextBlock Text="Hold this key to open the launcher overlay, hover over a card, and release to trigger." 
                                       Foreground="#8E8E93" FontSize="11" TextWrapping="Wrap" LineHeight="16"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Metrics Card -->
                <Border x:Name="CardMetrics" Background="#131315" BorderBrush="#262629" BorderThickness="1" CornerRadius="12" Padding="20" Margin="8">
                    <StackPanel>
                        <TextBlock Text="CANVAS OVERVIEW" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,16"/>
                        
                        <Grid>
                            <StackPanel>
                                <TextBlock x:Name="TxtShortcutCount" Text="0" Foreground="#F3F3F3" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Shortcuts configured" Foreground="#8E8E93" FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="0,20,0,0">
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Start with Windows" Foreground="#8E8E93" FontSize="12"/>
                                <TextBlock x:Name="TxtStartupStatus" Text="Disabled" Foreground="#F3F3F3" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Backdrop Dim" Foreground="#8E8E93" FontSize="12"/>
                                <TextBlock x:Name="TxtDimStatus" Text="Enabled" Foreground="#F3F3F3" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Quick Actions Card -->
                <Border x:Name="CardActions" Background="#131315" BorderBrush="#262629" BorderThickness="1" CornerRadius="12" Padding="20" Margin="8">
                    <StackPanel>
                        <TextBlock Text="QUICK ACTIONS" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,16"/>
                        
                        <WrapPanel Orientation="Horizontal" Margin="-6">
                            <Button Content="Test Overlay" Style="{StaticResource AccentButtonStyle}" Width="135" Margin="6" Padding="10,12" Click="BtnTestOverlay_Click"/>
                            <Button Content="Edit Canvas Layout" Style="{StaticResource DefaultButtonStyle}" Width="145" Margin="6" Padding="10,12" Click="BtnEditCanvas_Click"/>
                            <Button Content="Add New Shortcut" Style="{StaticResource DefaultButtonStyle}" Width="140" Margin="6" Padding="10,12" Click="BtnAddShortcut_Click"/>
                            <Button Content="Configure Trigger" Style="{StaticResource DefaultButtonStyle}" Width="140" Margin="6" Padding="10,12" Click="BtnChangeTrigger_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
