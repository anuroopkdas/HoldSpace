<Window x:Class="HoldSpace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoldSpace"
        xmlns:views="clr-namespace:HoldSpace.Views"
        mc:Ignorable="d"
        Title="HoldSpace" Height="680" Width="1000" MinHeight="650" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#0C0C0D" Foreground="#FFFFFF">
    
    <Window.Resources>
        <!-- Sleek Custom Styles -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite" ContentSource="Header" 
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#00E5FF"/>
                                <Setter Property="BorderBrush" Value="#00E5FF"/>
                                <Setter Property="Background" Value="#1E1E1E"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer layout: title bar row + content row -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="38"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══ Custom Title Bar ══ -->
        <Grid Grid.Row="0" x:Name="TitleBar" Background="#0C0C0D"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- App name -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                <TextBlock Text="Hold" Foreground="#F3F3F3" FontSize="13" FontWeight="Bold"/>
                <TextBlock Text="Space" Foreground="#0A84FF" FontSize="13" FontWeight="Bold"/>
            </StackPanel>

            <!-- Window chrome buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch">
                <Button x:Name="BtnMinimize" Width="46" Content="─"
                        Foreground="#8E8E93" Background="Transparent" BorderThickness="0"
                        FontSize="11" Cursor="Hand" Click="BtnMinimize_Click">
                    <Button.Style><Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value><ControlTemplate TargetType="Button">
                                <Border x:Name="Bg" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bg" Property="Background" Value="#1E1E1E"/></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate></Setter.Value>
                        </Setter>
                    </Style></Button.Style>
                </Button>
                <Button x:Name="BtnMaximize" Width="46" Content="□"
                        Foreground="#8E8E93" Background="Transparent" BorderThickness="0"
                        FontSize="11" Cursor="Hand" Click="BtnMaximize_Click">
                    <Button.Style><Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value><ControlTemplate TargetType="Button">
                                <Border x:Name="Bg" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bg" Property="Background" Value="#1E1E1E"/></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate></Setter.Value>
                        </Setter>
                    </Style></Button.Style>
                </Button>
                <Button x:Name="BtnClose" Width="46" Content="✕"
                        Foreground="#8E8E93" Background="Transparent" BorderThickness="0"
                        FontSize="11" Cursor="Hand" Click="BtnClose_Click">
                    <Button.Style><Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value><ControlTemplate TargetType="Button">
                                <Border x:Name="Bg" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bg" Property="Background" Value="#C42B1C"/><Setter Property="Foreground" Value="#FFFFFF"/></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate></Setter.Value>
                        </Setter>
                    </Style></Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <!-- ══ Content area (sidebar + main) ══ -->
        <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <!-- Sidebar Navigation Area -->
            <ColumnDefinition Width="240"/>
            <!-- Separator Line -->
            <ColumnDefinition Width="1"/>
            <!-- Content Screen Area -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation Panel -->
        <Grid Grid.Column="0" Background="{StaticResource CardBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Sidebar Title Header -->
            <StackPanel Grid.Row="0" Margin="24,28,24,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Hold" Foreground="{StaticResource TextPrimary}" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="Space" Foreground="{StaticResource AccentBrush}" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>

            <!-- Sidebar Navigation Menu Items -->
            <ListBox x:Name="SidebarMenu" Grid.Row="1" Style="{StaticResource SidebarListBox}" SelectedIndex="0" Margin="10,8">
                <ListBoxItem>Overview</ListBoxItem>
                <ListBoxItem>Modes</ListBoxItem>
                <ListBoxItem>Canvas Editor</ListBoxItem>
                <ListBoxItem>Trigger Options</ListBoxItem>
                <ListBoxItem>Appearance Options</ListBoxItem>
                <ListBoxItem>System Settings</ListBoxItem>
            </ListBox>

            <!-- Sidebar Footer Status Panel -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,16">
                <StackPanel>
                    <TextBlock x:Name="StatusText" Text="Status: Idle" Foreground="{StaticResource TextPrimary}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="Running in background" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Subtle vertical border line separator -->
        <Border Grid.Column="1" Background="{StaticResource BorderBrush}"/>

        <!-- Main Views Grid (linked to ListBox selection) -->
        <Grid Grid.Column="2" Margin="24">
            <TabControl x:Name="MainTabControl" Style="{StaticResource HiddenTabControl}"
                        SelectedIndex="{Binding ElementName=SidebarMenu, Path=SelectedIndex}">
                
                <!-- Overview View -->
                <TabItem>
                    <views:OverviewView x:Name="OverviewViewControl"/>
                </TabItem>

                <!-- Modes View -->
                <TabItem>
                    <views:ProfilesView x:Name="ProfilesViewControl"/>
                </TabItem>

                <!-- Canvas Editor View -->
                <TabItem>
                    <views:CanvasEditorView x:Name="CanvasEditor"/>
                </TabItem>

                <!-- Trigger View -->
                <TabItem>
                    <views:TriggerSettingsView x:Name="TriggerSettings"/>
                </TabItem>

                <!-- Appearance View -->
                <TabItem>
                    <views:AppearanceSettingsView x:Name="AppearanceSettings"/>
                </TabItem>

                <!-- General App Settings View -->
                <TabItem>
                    <views:GeneralSettingsView x:Name="GeneralSettings"/>
                </TabItem>

            </TabControl>
        </Grid>
        </Grid><!-- /content grid -->
    </Grid><!-- /outer grid -->
</Window>
