<Window x:Class="HoldSpace.AddShortcutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoldSpace"
        mc:Ignorable="d"
        Title="Add Shortcut" Height="480" Width="550"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Border Background="#0C0C0D" BorderBrush="#262629" BorderThickness="1" CornerRadius="12">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <!-- Title Bar Area -->
                <RowDefinition Height="Auto"/>
                <!-- Form Area -->
                <RowDefinition Height="*"/>
                <!-- Footer buttons / Error banner -->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Add Shortcut" Foreground="#F3F3F3" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="Configure a new action to place on your canvas." Foreground="#8E8E93" FontSize="11" Margin="0,3,0,0"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="0" Height="1" Background="#262629" Margin="0,28,0,0"/>

            <!-- Form Split Grid -->
            <Grid Grid.Row="1" Margin="0,16">
                <Grid.ColumnDefinitions>
                    <!-- Left Type ListBox -->
                    <ColumnDefinition Width="145"/>
                    <!-- Separator Line -->
                    <ColumnDefinition Width="1"/>
                    <!-- Right Input Area -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Type Menu -->
                <ListBox x:Name="ListTypes" Grid.Column="0" Style="{StaticResource SidebarListBox}" SelectedIndex="0" Margin="0,0,8,0" SelectionChanged="ListTypes_SelectionChanged">
                    <ListBoxItem>🖥 App (.exe)</ListBoxItem>
                    <ListBoxItem>📁 Folder</ListBoxItem>
                    <ListBoxItem>📄 File</ListBoxItem>
                    <ListBoxItem>🌐 Website</ListBoxItem>
                    <ListBoxItem>⚙ System Action</ListBoxItem>
                </ListBox>

                <Border Grid.Column="1" Background="#262629"/>

                <!-- Right Input Panel (Toggled by ListTypes selection) -->
                <TabControl x:Name="TabFields" Grid.Column="2" Style="{StaticResource HiddenTabControl}" SelectedIndex="{Binding ElementName=ListTypes, Path=SelectedIndex}" Margin="12,0,0,0" Background="Transparent" BorderThickness="0">
                    
                    <!-- Tab 0: App (.exe) -->
                    <TabItem>
                        <StackPanel>
                            <TextBlock Text="Shortcut Name" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtAppName" Padding="8" Margin="0,0,0,16" TextChanged="ValidateInputs"/>

                            <TextBlock Text="Executable Path" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtAppPath" Grid.Column="0" Padding="8" TextChanged="TxtAppPath_TextChanged"/>
                                <Button Content="Browse" Style="{StaticResource DefaultButtonStyle}" Grid.Column="1" Margin="6,0,0,0" Padding="8,2" Click="BtnBrowseApp_Click"/>
                            </Grid>

                            <TextBlock Text="Arguments (Optional)" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtAppArguments" Padding="8"/>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 1: Folder -->
                    <TabItem>
                        <StackPanel>
                            <TextBlock Text="Shortcut Name" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtFolderName" Padding="8" Margin="0,0,0,16" TextChanged="ValidateInputs"/>

                            <TextBlock Text="Folder Path" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtFolderPath" Grid.Column="0" Padding="8" TextChanged="TxtFolderPath_TextChanged"/>
                                <Button Content="Browse" Style="{StaticResource DefaultButtonStyle}" Grid.Column="1" Margin="6,0,0,0" Padding="8,2" Click="BtnBrowseFolder_Click"/>
                            </Grid>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 2: File -->
                    <TabItem>
                        <StackPanel>
                            <TextBlock Text="Shortcut Name" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtFileName" Padding="8" Margin="0,0,0,16" TextChanged="ValidateInputs"/>

                            <TextBlock Text="File Path" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtFilePath" Grid.Column="0" Padding="8" TextChanged="TxtFilePath_TextChanged"/>
                                <Button Content="Browse" Style="{StaticResource DefaultButtonStyle}" Grid.Column="1" Margin="6,0,0,0" Padding="8,2" Click="BtnBrowseFile_Click"/>
                            </Grid>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 3: Website -->
                    <TabItem>
                        <StackPanel>
                            <TextBlock Text="Shortcut Name (Optional)" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtWebName" Padding="8" Margin="0,0,0,16" TextChanged="ValidateInputs"/>

                            <TextBlock Text="Website URL / Domain" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtWebUrl" Padding="8" Margin="0,0,0,8" TextChanged="TxtWebUrl_TextChanged"/>
                            <TextBlock Text="Example: chatgpt.com or https://google.com" Foreground="#8E8E93" FontSize="10"/>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 4: System Action -->
                    <TabItem>
                        <StackPanel>
                            <TextBlock Text="Shortcut Name" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtSysName" Padding="8" Margin="0,0,0,16" TextChanged="ValidateInputs"/>

                            <TextBlock Text="Select Action" Foreground="#8E8E93" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <ComboBox x:Name="ComboSysAction" Padding="8" SelectionChanged="ComboSysAction_SelectionChanged">
                                <ComboBoxItem Content="Open Settings"/>
                                <ComboBoxItem Content="Open Task Manager"/>
                                <ComboBoxItem Content="Open Terminal"/>
                                <ComboBoxItem Content="Lock Screen"/>
                                <ComboBoxItem Content="Show Desktop"/>
                                <ComboBoxItem Content="Sleep PC"/>
                                <ComboBoxItem Content="Hibernate PC"/>
                                <ComboBoxItem Content="Shutdown PC"/>
                                <ComboBoxItem Content="Restart PC"/>
                                <ComboBoxItem Content="Sign Out"/>
                            </ComboBox>
                        </StackPanel>
                    </TabItem>

                </TabControl>
            </Grid>

            <!-- Footer: error labels & submit actions -->
            <Border Grid.Row="2" BorderBrush="#262629" BorderThickness="0,1,0,0" Padding="0,14,0,0">
                <Grid>
                    <!-- Validation error block -->
                    <TextBlock x:Name="TxtError" Text="Please enter required fields." Foreground="#FF453A" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Visibility="Collapsed"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource DefaultButtonStyle}" Width="80" Margin="0,0,8,0" Click="BtnCancel_Click"/>
                        <Button x:Name="BtnAddShortcut" Content="Add Shortcut" Style="{StaticResource AccentButtonStyle}" Width="120" Click="BtnAddShortcut_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
